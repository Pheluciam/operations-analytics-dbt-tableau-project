# Copy to .env (gitignored) and/or set in your shell session before running dbt.
# PowerShell:  $env:DBT_PG_PASSWORD = "your_postgres_password"
# This is the only secret the project needs; profiles.yml reads it via env_var().
DBT_PG_PASSWORD=your_postgres_password_here
